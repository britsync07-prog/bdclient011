# bdclient011

Simple static front page for:

**PBBET: Next-Gen iGaming Ecosystem**

## Cloudflare Pages Settings

Use these settings when connecting this Git repository to Cloudflare Pages:

- Framework preset: `None`
- Build command: leave empty / no build command
- Build output directory: `/`
- Root directory: `/`

The page is served from `index.html`.
