---
phase: 1
plan: 1.3
wave: 3
depends_on: ["1.2"]
files_modified: ["Frontend/src/app/admin/page.tsx", "Frontend/src/app/admin/login/page.tsx", "server/src/controllers/adminController.js"]
autonomous: true
---

# Plan 1.3: Admin Dashboard & Financials

<objective>
Restore the Admin Dashboard with real data and secure login.

Purpose: Ensure admins can manage users, KYC, and finances using live data.
Output: Updated `AdminDashboard`, admin login, and backend `adminController.js`.
</objective>

<context>
- docs/superpowers/specs/2025-01-24-pbbet-fix-design.md
- Frontend/src/app/admin/page.tsx
- Frontend/src/app/admin/login/page.tsx
- server/src/controllers/adminController.js
</context>

<tasks>

<task type="auto">
  <name>Fix Admin Login</name>
  <files>Frontend/src/app/admin/login/page.tsx</files>
  <action>
    Replace hardcoded admin credentials with a call to the backend `/auth/login` endpoint.
    Only allow access if `data.user.role === 'ADMIN'`.
    Save the token and `isAdmin` flag in `localStorage`.
  </action>
  <verify>
    Test admin login with real credentials from the database.
  </verify>
  <done>Admin login is secure and uses backend authentication.</done>
</task>

<task type="auto">
  <name>Restore Admin Dashboard Data</name>
  <files>Frontend/src/app/admin/page.tsx</files>
  <action>
    Update all fetch requests to include the Bearer token from `localStorage`.
    Ensure the "Financial Desk" tab fetches and displays real transaction requests.
    Implement the "KYC Verification" tab logic to approve/reject real users.
    Calculate dashboard stats from the `users` and `transactions` data.
  </action>
  <verify>
    Verify that the dashboard shows real user counts and balances.
  </verify>
  <done>Admin dashboard displays live data from the database.</done>
</task>

<task type="auto">
  <name>Update Admin Controller for Financials</name>
  <files>server/src/controllers/adminController.js</files>
  <action>
    Ensure `getFinancialRequests` correctly filters for DEPOSIT/WITHDRAWAL types.
    Ensure `approveFinancialRequest` correctly updates the user balance in the database.
  </action>
  <verify>
    Test approving a deposit request and check if user balance increases.
  </verify>
  <done>Financial requests are correctly handled and balance is updated.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Admin login works with DB credentials.
- [ ] Admin dashboard shows real users.
- [ ] Approving a deposit updates user balance.
</verification>

<success_criteria>
- [ ] Admin authority is fully operational with live data.
</success_criteria>
