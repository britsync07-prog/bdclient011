---
phase: 1
plan: 1.1
wave: 1
depends_on: []
files_modified: ["server/src/controllers/authController.js", "server/src/controllers/userController.js"]
autonomous: true
---

# Plan 1.1: Backend Auth & Game Endpoints

<objective>
Update the backend authentication and game fetching endpoints to provide complete data to the frontend.

Purpose: Ensure the frontend receives the full user object and consistent game data.
Output: Updated `authController.js` and `userController.js`.
</objective>

<context>
- docs/superpowers/specs/2025-01-24-pbbet-fix-design.md
- server/src/controllers/authController.js
- server/src/controllers/userController.js
</context>

<tasks>

<task type="auto">
  <name>Update Auth Controller Login Response</name>
  <files>server/src/controllers/authController.js</files>
  <action>
    Modify the `login` function to return the full `user` object in the response.
    Include `id`, `username`, `role`, and `balance`.
    AVOID: Returning the password field.
  </action>
  <verify>
    Use curl to test login: `curl -X POST http://localhost:5000/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'` (assuming admin exists or use a known user).
  </verify>
  <done>Login response contains `token` and `user` object with role and balance.</done>
</task>

<task type="auto">
  <name>Standardize Game Controller Response</name>
  <files>server/src/controllers/userController.js</files>
  <action>
    Update the `getGames` function to return an object with a `games` property containing the array of games, to match the frontend `GameStoreContext.tsx` expectation.
    Also, ensure `getGames` correctly handles vendor list and fetches games in parallel (this seems mostly correct but double check the message structure from OroPlay).
  </action>
  <verify>
    Use curl to test games endpoint: `curl -H "Authorization: Bearer <token>" http://localhost:5000/api/user/games`
  </verify>
  <done>Games endpoint returns `{ games: [...] }`.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Login returns full user object.
- [ ] Games endpoint returns object with `games` array.
</verification>

<success_criteria>
- [ ] Backend provides necessary data for frontend restoration.
</success_criteria>
