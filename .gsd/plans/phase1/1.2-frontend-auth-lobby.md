---
phase: 1
plan: 1.2
wave: 2
depends_on: ["1.1"]
files_modified: ["Frontend/src/app/login/page.tsx", "Frontend/src/components/features/games/CasinoGameLobby.tsx"]
autonomous: true
---

# Plan 1.2: Frontend Auth & Game Lobby

<objective>
Fix the frontend authentication flow and game lobby state management.

Purpose: Ensure users can log in correctly and see their data, and that games are correctly launched.
Output: Updated `login/page.tsx` and `CasinoGameLobby.tsx`.
</objective>

<context>
- docs/superpowers/specs/2025-01-24-pbbet-fix-design.md
- Frontend/src/app/login/page.tsx
- Frontend/src/components/features/games/CasinoGameLobby.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix Frontend Login Logic</name>
  <files>Frontend/src/app/login/page.tsx</files>
  <action>
    Update `handleLogin` to correctly save the `user` object and set `isAdmin` in `localStorage` if `data.user.role === 'ADMIN'`.
    Ensure `localStorage.setItem("user", JSON.stringify(data.user))` uses the updated backend response.
  </action>
  <verify>
    Manually test login and check localStorage in the browser (if possible) or verify the next page shows user data.
  </verify>
  <done>Login correctly persists user data and admin status.</done>
</task>

<task type="auto">
  <name>Fix Game Lobby & Launch Logic</name>
  <files>Frontend/src/components/features/games/CasinoGameLobby.tsx</files>
  <action>
    Ensure `handlePlay` correctly passes `vendorCode` and `gameCode` to the backend.
    Update the user display to use the balance from the persisted user object.
    Ensure "Sign In / Register" buttons are correctly wired.
  </action>
  <verify>
    Verify "Play" button triggers the launch URL fetch and opens a new window.
  </verify>
  <done>Games launch correctly and user balance is displayed.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] User login persists role and balance.
- [ ] Lobby shows user balance when logged in.
- [ ] Play button launches a game.
</verification>

<success_criteria>
- [ ] Core player experience is functional.
</success_criteria>
